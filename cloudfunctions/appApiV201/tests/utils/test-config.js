// 测试配置（共享）
// 版本: 1.0.0

// 全局测试配置，所有测试文件共享
const TEST_CONFIG = {
  testCode: null,
  testToken: null,
  testOpenId: null,
  testUserId: null,
  testDynId: null,
  testCircleId: null,
  testTopic: null
};

module.exports = TEST_CONFIG;
